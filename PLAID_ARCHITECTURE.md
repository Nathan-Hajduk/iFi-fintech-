# Plaid Integration Architecture & Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        iFi Platform                              │
│                                                                   │
│  ┌───────────────┐      ┌─────────────┐      ┌───────────────┐ │
│  │   Frontend    │◄────►│   Backend   │◄────►│   Database    │ │
│  │               │      │             │      │               │ │
│  │ - onboarding  │      │ - Express   │      │ - PostgreSQL  │ │
│  │ - dashboard   │      │ - Node.js   │      │ - Encrypted   │ │
│  │ - Plaid Link  │      │ - API       │      │ - Tables      │ │
│  └───────┬───────┘      └──────┬──────┘      └───────────────┘ │
│          │                     │                                 │
└──────────┼─────────────────────┼─────────────────────────────────┘
           │                     │
           │                     │
           └────────┬────────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │    Plaid Service     │
         │                      │
         │ - OAuth Flow         │
         │ - Token Exchange     │
         │ - Transactions API   │
         │ - Webhooks           │
         │ - 12,000+ Banks      │
         └──────────────────────┘
```

---

## Onboarding Flow (5 Steps)

```
┌─────────────────┐
│  Step 1: Start  │
│    Purpose      │
│                 │
│ ┌─────────────┐ │
│ │ Personal    │ │
│ │ Business    │ │
│ │ Investing   │ │
│ │ Debt Mgmt   │ │
│ └─────────────┘ │
└────────┬────────┘
         │ Select & Continue
         ▼
┌─────────────────┐
│ Step 2: NEW!    │
│  Connect Bank   │
│                 │
│ ┌─────────────┐ │
│ │ 🛡️ Benefits │ │
│ │ 🔒 Security │ │
│ │ 🏦 Connect  │ │
│ │ or Skip     │ │
│ └─────────────┘ │
└────────┬────────┘
         │ Connect or Skip
         ▼
┌─────────────────┐
│  Step 3:        │
│  Income         │
│                 │
│ ┌─────────────┐ │
│ │ Annual $    │ │
│ │ Source      │ │
│ │ (varies by  │ │
│ │  purpose)   │ │
│ └─────────────┘ │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Step 4:        │
│  Expenses       │
│                 │
│ ┌─────────────┐ │
│ │ Housing     │ │
│ │ Utilities   │ │
│ │ Food        │ │
│ │ Transport   │ │
│ └─────────────┘ │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Step 5:        │
│  Investments    │
│                 │
│ ┌─────────────┐ │
│ │ Stocks      │ │
│ │ ETFs        │ │
│ │ Crypto      │ │
│ │ Portfolio $ │ │
│ └─────────────┘ │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  🎉 Complete!   │
│  → Dashboard    │
└─────────────────┘
```

---

## Plaid Connection Flow (Step 2 Detail)

```
User clicks "Connect Bank Account"
         │
         ▼
┌─────────────────────────────┐
│ Frontend: initPlaidLink()   │
│ Checks if handler exists    │
└─────────────┬───────────────┘
              │
              ▼ (if not initialized)
┌─────────────────────────────┐
│ Frontend: Fetch link_token  │
│ POST /api/plaid/create_link │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Backend: Create link_token  │
│ - Call Plaid API            │
│ - Include user_id, products │
│ - Return link_token         │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Frontend: Init Plaid.create │
│ - Pass link_token           │
│ - Define onSuccess callback │
│ - Define onExit callback    │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ plaidHandler.open()         │
│ → Plaid Link Modal Opens    │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ User Selects Bank           │
│ → "First Platypus Bank"     │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ User Logs In (OAuth)        │
│ - Username: user_good       │
│ - Password: pass_good       │
│ ⚠️ iFi NEVER sees credentials│
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ User Selects Accounts       │
│ ☑ Checking                  │
│ ☑ Savings                   │
│ ☐ Credit Card               │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Plaid: onSuccess Callback   │
│ - Receives public_token     │
│ - Receives account metadata │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Frontend: Exchange Token    │
│ POST /api/plaid/exchange... │
│ - Send public_token         │
│ - Send account details      │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Backend: Exchange Token     │
│ - Call Plaid API            │
│ - Receive access_token      │
│ - ENCRYPT access_token      │
│ - Store in database         │
│ - Return success + item_id  │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Frontend: Success UI        │
│ ✅ Green checkmark          │
│ 🎉 Success toast            │
│ ⏱️ Wait 2 seconds           │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Auto-advance to Step 3      │
│ (Income)                    │
└─────────────────────────────┘
```

---

## Transaction Sync Flow (Ongoing)

```
Plaid detects new transactions
         │
         ▼
┌─────────────────────────────┐
│ Plaid: Send Webhook         │
│ POST /api/plaid/webhook     │
│ - webhook_type: TRANSACTIONS│
│ - webhook_code: DEFAULT_    │
│   UPDATE                    │
│ - item_id: item_abc123      │
│ - new_transactions: 15      │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Backend: Webhook Handler    │
│ - Verify signature          │
│ - Identify user from item_id│
│ - Decrypt access_token      │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Backend: Fetch Transactions │
│ POST /transactions/get      │
│ - access_token              │
│ - start_date (last 30 days) │
│ - end_date (today)          │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Plaid: Return Transactions  │
│ [                           │
│   {                         │
│     transaction_id,         │
│     amount,                 │
│     date,                   │
│     name,                   │
│     merchant_name           │
│   }                         │
│ ]                           │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Backend: Process Each Txn   │
│ - Check if exists in DB     │
│ - Categorize with AI        │
│   (Groceries, Dining, etc.) │
│ - Store in transactions     │
│   table                     │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Backend: Update Dashboard   │
│ - Recalculate budget        │
│ - Update spending trends    │
│ - Check for anomalies       │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Backend: Notify User        │
│ - Email if budget exceeded  │
│ - Push notification for     │
│   unusual spending          │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ User: View Updated Data     │
│ - Transactions page refreshes│
│ - Budget page updates       │
│ - Dashboard shows real-time │
│   insights                  │
└─────────────────────────────┘
```

---

## Data Storage Architecture

```
┌──────────────────────────────────────────────────────┐
│                  PostgreSQL Database                  │
├──────────────────────────────────────────────────────┤
│                                                       │
│  ┌─────────────────────┐    ┌──────────────────────┐│
│  │  plaid_connections  │    │    transactions      ││
│  │                     │    │                      ││
│  │ - id (PK)           │    │ - id (PK)            ││
│  │ - user_id (FK)      │    │ - user_id (FK)       ││
│  │ - item_id (unique)  │    │ - transaction_id     ││
│  │ - access_token ⚠️   │    │   (unique)           ││
│  │   (ENCRYPTED!)      │    │ - account_id         ││
│  │ - institution_name  │    │ - amount             ││
│  │ - accounts (JSONB)  │    │ - date               ││
│  │ - status            │    │ - name               ││
│  │ - created_at        │    │ - merchant_name      ││
│  └─────────────────────┘    │ - category           ││
│                              │ - pending (bool)     ││
│                              │ - created_at         ││
│                              └──────────────────────┘│
│                                                       │
│  Indexes:                                            │
│  - plaid_connections.user_id (fast user lookup)     │
│  - plaid_connections.item_id (fast Plaid lookup)    │
│  - transactions.user_id (fast user transactions)    │
│  - transactions.date (fast date range queries)      │
│  - transactions.category (fast category filtering)  │
└──────────────────────────────────────────────────────┘
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────┐
│               Security Architecture                  │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Layer 1: User Authentication                       │
│  ┌──────────────────────────────────────────┐      │
│  │ iFi Login → JWT Token → Session           │      │
│  │ ⚠️ User must be logged in to connect bank │      │
│  └──────────────────────────────────────────┘      │
│                                                      │
│  Layer 2: Plaid OAuth (Bank Login)                 │
│  ┌──────────────────────────────────────────┐      │
│  │ Bank credentials → Plaid servers ONLY      │      │
│  │ ⚠️ iFi NEVER sees username/password       │      │
│  └──────────────────────────────────────────┘      │
│                                                      │
│  Layer 3: Token Exchange                           │
│  ┌──────────────────────────────────────────┐      │
│  │ public_token (temporary, 30 min)          │      │
│  │    ↓                                      │      │
│  │ access_token (permanent, encrypted)       │      │
│  │ ⚠️ Encrypted with AES-256 before storage  │      │
│  └──────────────────────────────────────────┘      │
│                                                      │
│  Layer 4: Data Transmission                        │
│  ┌──────────────────────────────────────────┐      │
│  │ All API calls → HTTPS/TLS 1.3             │      │
│  │ ⚠️ No plain HTTP in production            │      │
│  └──────────────────────────────────────────┘      │
│                                                      │
│  Layer 5: Database Security                        │
│  ┌──────────────────────────────────────────┐      │
│  │ - Encrypted at rest (database level)      │      │
│  │ - Access tokens encrypted (app level)     │      │
│  │ - Database credentials in .env            │      │
│  │ ⚠️ Never commit .env to git               │      │
│  └──────────────────────────────────────────┘      │
│                                                      │
│  Layer 6: Webhook Verification                     │
│  ┌──────────────────────────────────────────┐      │
│  │ Plaid webhooks → HMAC signature           │      │
│  │ ⚠️ Verify signature before processing     │      │
│  └──────────────────────────────────────────┘      │
│                                                      │
│  Layer 7: Rate Limiting                            │
│  ┌──────────────────────────────────────────┐      │
│  │ - 100 requests / 15 minutes               │      │
│  │ - Per user, per IP                        │      │
│  │ ⚠️ Prevents abuse and DDoS                │      │
│  └──────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
Error occurs during connection
         │
         ├─────────────┬─────────────┬─────────────┐
         │             │             │             │
         ▼             ▼             ▼             ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ Link token  │ │ User exited │ │ Bank error  │ │ Network err │
│ failed      │ │ without     │ │ (invalid    │ │ (timeout)   │
│             │ │ connecting  │ │ credentials)│ │             │
└──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────┬──────┘
       │               │               │               │
       ▼               ▼               ▼               ▼
┌─────────────────────────────────────────────────────────┐
│          Frontend: Error Display                        │
│                                                          │
│  - Show toast notification (red)                        │
│  - Keep "Connect Bank Account" button active            │
│  - Allow retry or skip                                  │
│  - Log error to console (debugging)                     │
└──────────────────────────────┬──────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────┐
│          User Actions                                    │
│                                                          │
│  1. Retry → Click "Connect Bank Account" again          │
│  2. Skip → Click "Skip for now" → Proceed to Step 3    │
│  3. Back → Return to Step 1, change purpose            │
└─────────────────────────────────────────────────────────┘
```

---

## Performance Optimization

```
┌─────────────────────────────────────────────────────┐
│              Performance Strategy                    │
├─────────────────────────────────────────────────────┤
│                                                      │
│  1. Lazy Loading                                    │
│     - Plaid SDK loaded only on onboarding page     │
│     - Not loaded on dashboard (reduces bundle)     │
│                                                      │
│  2. Token Caching                                   │
│     - link_token cached for 30 minutes             │
│     - Don't fetch new token on every page reload   │
│                                                      │
│  3. Transaction Batching                            │
│     - Fetch 500 transactions per API call          │
│     - Use pagination for older transactions        │
│                                                      │
│  4. Database Indexing                               │
│     - user_id indexed (fast user lookups)          │
│     - date indexed (fast date range queries)       │
│     - transaction_id indexed (fast duplicate check)│
│                                                      │
│  5. Webhook Processing                              │
│     - Process webhooks asynchronously              │
│     - Queue transactions for background processing │
│     - Don't block webhook response                 │
│                                                      │
│  6. Frontend Optimization                           │
│     - Use CSS animations (GPU accelerated)         │
│     - Minimize DOM updates                         │
│     - Debounce input fields                        │
└─────────────────────────────────────────────────────┘
```

---

## Testing Checklist

```
Frontend Testing (Without Backend)
☑ Progress bar shows 5 steps
☑ Step 2 displays bank connection UI
☑ Security badges render correctly
☑ Benefits list displays with icons
☑ "Connect Bank Account" button styled
☑ "Skip for now" link present
☑ Mobile responsive (< 768px)
☑ Error message shows when backend missing

Integration Testing (With Backend)
☐ link_token creation succeeds
☐ Plaid Link modal opens
☐ Can select test bank
☐ OAuth login works (user_good/pass_good)
☐ Account selection shows
☐ public_token exchange succeeds
☐ access_token encrypted and stored
☐ Success UI displays (green checkmark)
☐ Auto-advance to Step 3 works
☐ Skip button advances to Step 3

Backend Testing
☐ Create link_token endpoint responds
☐ Exchange token endpoint works
☐ access_token encrypted properly
☐ Database entry created correctly
☐ Webhook endpoint receives events
☐ Transactions sync successfully
☐ Error handling works (invalid token)
☐ Rate limiting prevents abuse

Security Testing
☐ access_token never exposed in API
☐ Webhook signatures verified
☐ HTTPS enforced in production
☐ .env not committed to git
☐ Database credentials secure
☐ SQL injection prevention
☐ XSS prevention in transaction names
```

---

## Monitoring & Alerts

```
┌─────────────────────────────────────────────────────┐
│              Monitoring Dashboard                    │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Metrics to Track:                                  │
│  ┌────────────────────────────────────────────┐    │
│  │ 1. Connection Success Rate                 │    │
│  │    - Target: > 85%                         │    │
│  │    - Alert if < 70% for 1 hour             │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │ 2. Transaction Sync Latency                │    │
│  │    - Target: < 5 seconds                   │    │
│  │    - Alert if > 30 seconds                 │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │ 3. Webhook Processing Time                 │    │
│  │    - Target: < 2 seconds                   │    │
│  │    - Alert if > 10 seconds                 │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │ 4. Failed Token Exchanges                  │    │
│  │    - Target: < 5% failure rate             │    │
│  │    - Alert if > 10% for 15 minutes         │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │ 5. ITEM_LOGIN_REQUIRED Events              │    │
│  │    - Track reauth rate                     │    │
│  │    - Alert if > 20% of connections need it │    │
│  └────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

---

**Visual Flow Documentation Complete**  
**Status:** Ready for development handoff  
**Next:** Begin backend implementation
